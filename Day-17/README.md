# Day 17 - 30 Days Python Challenge 🐍

## What I Learned Today

- Text to Speech using `os` module
- Working with the `time` module

## Programs Written

### 1. Shoutout System
A program that takes multiple names as input and uses the `os` module to make
the computer speak out a shoutout for each person.

### 2. ATM System
A complete ATM simulation with balance check, deposit, and withdraw features,
using the `time` module to calculate and display the total session duration.

## Concepts Covered
- Text-to-Speech using `os.system()`
- The `time` module (`time.time()`)
- Dictionary-based data storage
- Loops, conditionals, and session-based programs

---
Part of my #30DaysPythonChallenge 🚀
