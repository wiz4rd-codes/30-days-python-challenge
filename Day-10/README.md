# Day 10 - 30 Days Python Challenge 🐍

## What I Learned Today
- Class and Instance Variables & Methods
- Class Variables and Classmethods
- File handling using the `os` module

## Programs Written

### 1. File Renamer
A program that automatically renames all files with a given extension inside a
folder, using the `os` module to read and rename files sequentially.

### 2. Gym Member System
A program that manages gym members using class and instance variables. Uses a
`classmethod` to update the shared membership fee across all members at once.

## Concepts Covered
- `os.listdir()` and `os.rename()`
- File extension handling with `os.path.splitext()`
- Class variables vs instance variables
- `@classmethod` decorator
- Object list management with `enumerate()`

---
Part of my #30DaysPythonChallenge 🚀
